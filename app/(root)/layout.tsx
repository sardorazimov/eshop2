'use client'
// 'use server'
// root conntrol  page 
// user page layout 
// server actions
// control server
//

const RLayout = ({children}:{children: React.ReactNode}) => {
  return (
    <main>
      {children}
    </main>
  )
}

export default RLayout
